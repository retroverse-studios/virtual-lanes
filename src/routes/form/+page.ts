// Static "coming soon" page: prerender for instant first paint + crawlability.
// The rest of the app stays an offline-first SPA (see +layout.ts).
export const prerender = true;
export const ssr = true;

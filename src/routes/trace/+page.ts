// Prerendered shell for instant first paint; the clip loader hydrates client-side.
// The rest of the app stays an offline-first SPA (see +layout.ts).
export const prerender = true;
export const ssr = true;
